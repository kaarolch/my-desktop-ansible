---
# System_utils playbook

- name: Install system utils packages
  dnf:
    name: "{{ item }}"
    state: latest
  with_items:
    - curl
    - gitk
    - htop
    - iotop
    - nc
    - nmap
    - sysstat
    - telnet
    - vim
    - virty-manager
    - wget
